import styled from 'styled-components'

export const UserInfoWrapper = styled.div`
  display: flex;
  .form {
    flex: 2;
  }

  .avatar {
    flex: 1;
    display: flex;
    justify-content: center;
  }
`
